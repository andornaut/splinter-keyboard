{
  "dependencies": {
    "ergogen": "^4.0.2"
  },
  "devDependencies": {
      "nodemon": "^2.0.22"
  },
  "scripts": {
    "build": "npx ergogen ergogen/ --output dist",
    "watch": "nodemon --ext .js,.yaml --watch ergogen/config.yaml --exec npm run build"
  }
}
