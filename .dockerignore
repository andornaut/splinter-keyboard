# The image only needs our custom footprints (ergogen/footprints/*.js);
# everything else just bloats the build context. The ceoloide/infused-kim
# submodules are cloned by the GUI's own patch flow, not copied from here.
.git
.github
dist
ergogen/footprints/ceoloide
ergogen/footprints/infused-kim
node_modules
**/node_modules
v[0-9]*
*.md
.gitignore
.gitmodules
Dockerfile
.dockerignore
docker-compose.yaml
